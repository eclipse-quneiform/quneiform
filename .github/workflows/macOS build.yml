name: macOS Build
on: [push,pull_request]

jobs:
  build:
    name: build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'
      - name: build
        run: |
             cmake ./
             make -j4
